/**
 * Operator representation within program.
 */
export enum Operator {
    PLUS = '+',
    MINUS = '-',
    OPEN_BRACKET = '(',
    CLOSE_BRACKET = ')',
    MULTIPLY = '*',
    DIVIDE = '/',
    POWER = '^',
};